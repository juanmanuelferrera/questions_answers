name = "vedabase-synthesis"
main = "src/synthesis-worker.ts"
compatibility_date = "2024-01-01"

# API Keys are stored as Cloudflare Secrets (not in this file for security)
# To set them, use:
#   wrangler secret put OPENROUTER_API_KEY --config wrangler.synthesis.toml
#   wrangler secret put OPENAI_API_KEY --config wrangler.synthesis.toml
#
# The secrets will be encrypted and stored securely in Cloudflare's infrastructure

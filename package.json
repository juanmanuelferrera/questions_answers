{
  "name": "philosophy-rag",
  "version": "1.0.0",
  "description": "RAG system for comparative philosophy corpus",
  "scripts": {
    "deploy": "wrangler deploy",
    "deploy-import": "wrangler deploy --name philosophy-rag-import src/import-worker.ts",
    "dev": "wrangler dev",
    "dev-import": "wrangler dev src/import-worker.ts"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20240129.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "wrangler": "^3.23.0"
  }
}
